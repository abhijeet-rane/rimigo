export const ACTIVITIES_CONFIG = {
    WHERE: {
        placeholder: 'Select a place, city or country'
    },
    WHEN: {
        placeholder: 'Select month'
    },
    PREFERENCES: {
        placeholder: 'Select preferences'
    }
}
