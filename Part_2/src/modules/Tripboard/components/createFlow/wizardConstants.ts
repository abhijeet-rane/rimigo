/** Central max-width class for all wizard steps — keeps content consistently sized.
 *  Lives in its own module (not TripboardCreateFlow) so that big component file
 *  exports only a component and stays Fast-Refresh-eligible. */
export const WIZARD_CONTENT_MAX_WIDTH = 'max-w-4xl'
